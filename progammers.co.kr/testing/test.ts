// 숫자 문자열과 영단어
// 다음은 숫자의 일부 자릿수를 영단어로 바꾸는 예시입니다.

// 문제 설명

type caseProps = { param1: string };
const testCase: caseProps = {
  param1: "23four5six7",
};

function solution(s: caseProps["param1"]) {
  return null;
}

console.log(solution(testCase.param1));
