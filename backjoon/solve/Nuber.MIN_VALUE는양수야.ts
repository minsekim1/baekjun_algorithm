// const fs = require("fs");
// const inputList: string[] = fs.readFileSync("./dev/stdin").toString().split("\n");
// // const input: string = fs.readFileSync("./dev/stdin").toString();

// const x = Number(inputList[0]);
// const list = inputList[1].split(" ");
// let max = Number.MIN_VALUE; //<-이거 양수중 가장 작은 값임
// let min = Number.MAX_VALUE;

// list.map((item) => {
//   const n = Number(item);
//   if (max < n) max = n;
//   if (min > n) min = n;
// });
// console.log(min + " " + max);

// ==========================================

// const fs = require("fs");
// const inputList: string[] = fs.readFileSync("./dev/stdin").toString().split("\n");
// // const input: string = fs.readFileSync("./dev/stdin").toString();

// const x = Number(inputList[0]);
// const list = inputList[1].split(" ");
// let max = Number.MIN_SAFE_INTEGER;
// let min = Number.MAX_SAFE_INTEGER;

// list.map((item) => {
//   const n = Number(item);
//   if (max < n) max = n;
//   if (min > n) min = n;
// });
// console.log(min + " " + max);
