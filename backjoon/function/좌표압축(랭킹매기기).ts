// 인풋 : [2, 4, -10, 4, -9]
// 아웃품: 0부터 랭킹별 순위 [2,3,0,3,1]

//   const line = inputList[1].split(" ").map(Number);

// const rankList = (line: number[]) => {
//   const set = new Set<number>();
//   const map = new Map<number, number>();
//   line.map((item) => set.add(item));
//   const array = Array.from(set);
//   array.sort((a, b) => a - b);
//   array.map((item, i) => map.set(item, i));
//   return line.map((item) => map.get(item));
// };
