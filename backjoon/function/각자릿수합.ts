// const numberSum = (n: number, init = 0) => {
//   let decompositionSum = init;
//   for (let temp = n; temp > 0; temp = Math.floor(temp / 10)) decompositionSum += temp % 10;
//   return decompositionSum;
// };
